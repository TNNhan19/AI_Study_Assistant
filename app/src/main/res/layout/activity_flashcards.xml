<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Header -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:background="@drawable/bg_gradient"
        android:orientation="vertical" android:padding="@dimen/spacing_lg"
        android:paddingTop="48dp">
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:gravity="center_vertical">
            <ImageButton android:id="@+id/btn_back"
                android:layout_width="40dp" android:layout_height="40dp"
                android:src="@android:drawable/ic_media_previous"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:tint="@color/text_on_primary" android:contentDescription="Back"/>
            <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:orientation="vertical"
                android:paddingStart="@dimen/spacing_sm">
                <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:text="Flashcards" android:textColor="@color/text_on_primary"
                    android:textSize="@dimen/text_xl" android:fontFamily="sans-serif-medium"/>
                <TextView android:id="@+id/tv_card_count"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:text="Card 1 of 10" android:textColor="#CCffffff"
                    android:textSize="@dimen/text_sm"/>
            </LinearLayout>
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_generate"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="Generate" android:textColor="@color/text_on_primary"
                app:strokeColor="@color/text_on_primary"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Flashcard Content -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="0dp"
        android:layout_weight="1" android:orientation="vertical"
        android:gravity="center" android:padding="@dimen/spacing_lg">

        <!-- Loading -->
        <LinearLayout android:id="@+id/layout_loading"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical" android:gravity="center" android:visibility="gone">
            <ProgressBar android:layout_width="48dp" android:layout_height="48dp"/>
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="Generating flashcards..." android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_md" android:layout_marginTop="@dimen/spacing_md"/>
        </LinearLayout>

        <!-- Flip Card Container (FrameLayout for animation) -->
        <FrameLayout android:id="@+id/flip_card_container"
            android:layout_width="match_parent" android:layout_height="@dimen/flashcard_height">

            <!-- Front of card -->
            <LinearLayout android:id="@+id/card_front"
                android:layout_width="match_parent" android:layout_height="match_parent"
                android:orientation="vertical" android:gravity="center"
                android:background="@drawable/bg_flashcard_front"
                android:padding="@dimen/spacing_lg">
                <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:text="QUESTION" android:textColor="#88FFFFFF"
                    android:textSize="@dimen/text_xs" android:letterSpacing="0.2"
                    android:fontFamily="sans-serif-medium"/>
                <TextView android:id="@+id/tv_front_text"
                    android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:text="What is TCP?" android:textColor="@color/text_on_primary"
                    android:textSize="@dimen/text_xxl" android:fontFamily="sans-serif-medium"
                    android:gravity="center" android:layout_marginTop="@dimen/spacing_md"
                    android:lineSpacingMultiplier="1.3"/>
                <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:text="Tap to flip →" android:textColor="#88FFFFFF"
                    android:textSize="@dimen/text_sm" android:layout_marginTop="@dimen/spacing_lg"/>
            </LinearLayout>

            <!-- Back of card -->
            <LinearLayout android:id="@+id/card_back"
                android:layout_width="match_parent" android:layout_height="match_parent"
                android:orientation="vertical" android:gravity="center"
                android:background="@drawable/bg_flashcard_back"
                android:padding="@dimen/spacing_lg"
                android:visibility="gone">
                <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:text="ANSWER" android:textColor="#88FFFFFF"
                    android:textSize="@dimen/text_xs" android:letterSpacing="0.2"
                    android:fontFamily="sans-serif-medium"/>
                <TextView android:id="@+id/tv_back_text"
                    android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:text="TCP is a connection-oriented protocol that ensures reliable data transmission."
                    android:textColor="@color/text_on_primary" android:textSize="@dimen/text_lg"
                    android:gravity="center" android:layout_marginTop="@dimen/spacing_md"
                    android:lineSpacingMultiplier="1.4"/>
            </LinearLayout>
        </FrameLayout>

        <!-- Progress Dots -->
        <LinearLayout android:id="@+id/layout_progress_dots"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:orientation="horizontal" android:gravity="center"
            android:layout_marginTop="@dimen/spacing_lg"/>

        <!-- Action Buttons -->
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:gravity="center"
            android:layout_marginTop="@dimen/spacing_lg">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_prev"
                style="@style/ButtonOutlined"
                android:layout_width="0dp" android:layout_height="@dimen/btn_height"
                android:layout_weight="1" android:text="← Prev"
                android:layout_marginEnd="@dimen/spacing_xs"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_next"
                style="@style/ButtonPrimary"
                android:layout_width="0dp" android:layout_height="@dimen/btn_height"
                android:layout_weight="1" android:text="Next →"
                android:layout_marginStart="@dimen/spacing_xs"/>
        </LinearLayout>

        <!-- Know / Don't Know -->
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:gravity="center"
            android:layout_marginTop="@dimen/spacing_sm">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_unknown"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="Need Review 🔄"
                android:textColor="@color/status_failed"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_known"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="I Know This ✓"
                android:textColor="@color/quiz_correct"/>
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
